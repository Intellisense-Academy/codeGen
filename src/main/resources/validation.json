{
  "type": "object",
  "title" : "Student",
  "properties": {
    "name": {
      "type": "string",
	  "minLength": 2,
	  "pattern": "^[A-Za-z]+$"
	},
    "age": {
      "type": "integer",
	  "minimum": 18,
	  "maximum": 80
    }
  },
  "required": ["name", "age"],
  "minProperties": 2
}